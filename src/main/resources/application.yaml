spring:
  application:
    name: SAMDI

  datasource:
    url: ${DB_URL:jdbc:mariadb://svc.sel3.cloudtype.app:31859/}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
